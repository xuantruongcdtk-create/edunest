/**
 * @deprecated File renamed for clarity — import from `gemini.client.ts` instead.
 * This re-export exists to avoid breaking any existing imports during migration.
 */
export * from './gemini.client'
